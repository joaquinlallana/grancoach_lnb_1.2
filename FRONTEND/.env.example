# URL del backend para producción.
# En desarrollo, Vite proxea /api → http://localhost:3000 (ver vite.config.js).
# En producción, configurar con el dominio del backend desplegado.
VITE_API_URL=http://localhost:3000
